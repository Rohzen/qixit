{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",

    "android:use:devconfig": "powershell -Command Copy-Item -Force capacitor.config.dev.json capacitor.config.json",
    "android:use:prodconfig": "powershell -Command Copy-Item -Force capacitor.config.prod.json capacitor.config.json",

    "android:dev": "vite --host",
    "android:sync": "npx cap sync android",
    "android:open": "npx cap open android",

    "android:setup": "npm run build && npx cap sync android && npx cap open android"
  },
  "devDependencies": {
    "vite": "^5.3.0",
    "@capacitor/cli": "^6.0.0"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0"
  }
}
